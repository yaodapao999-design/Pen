# Unity context access policy

- 默认不要读取 Unity 序列化文件：`.unity`（场景）、`.prefab`（预制体）。
- 只有在用户明确请求“查看/读取场景文件/预制体”等，或用户点名具体路径（例如 `Assets/.../*.unity` / `Assets/.../*.prefab`）时，才允许打开并引用这些文件内容。
- 若未明确授权但你判断必须读取，请先询问并说明原因与将读取的文件列表。
- `.meta` 文件不需要读取，除非用户明确要求排查 GUID/引用问题。
